









SELECT m.service_id, CONCAT(n.preff,n.orgid,n.code) from pj_enter_main m
inner join (
SELECT id, left(service_id, 3) preff, (100000 + orgid) as orgid, right(service_id,11) as code 
from pj_enter_main) n
on m.id = n.id
where LENGTH(service_id) = 14
