<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chedao.websocket.webserver.user.dao.UserTypeDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.chedao.websocket.webserver.user.model.UserTypeEntity" id="UserTypeMap">
        <result property="id" column="id"/>
        <result property="userid" column="userid"/>
        <result property="name" column="name"/>
        <result property="buildtime" column="buildtime"/>
    </resultMap>

    <sql id="Base_Column_List" >
  	  	     id ,
  	  	     userid ,
  	  	     name,
  	  	     buildtime

  </sql>

   <!-- 查找该用户所有分组-->
    <select id="queryList" resultType="com.chedao.websocket.webserver.user.model.UserTypeEntity" parameterType="map">
        select  <include refid="Base_Column_List" />  from user_type where user_id = #{userid}
    </select>

    <!-- <select id="queryByUid"  parameterType="long" resultType="com.chedao.websocket.webserver.user.model.UserInfoEntity">
         select  <include refid="Base_Column_List" />  from user_info where id = #{id}
     </select>

     <select id="queryList" resultType="com.chedao.websocket.webserver.user.model.UserInfoEntity">
         select  <include refid="Base_Column_List" />  from user_info
         <choose>
             <when test="sidx != null and sidx.trim() != ''">
                 order by ${sidx} ${order}
             </when>
             <otherwise>
                 order by id desc
             </otherwise>
         </choose>
         <if test="offset != null and limit != null">
             limit #{offset}, #{limit}
         </if>
     </select>-->

    <!--<select id="queryTotal" resultType="int">
		select count(*) from user_info
	</select>-->

    <insert id="save" parameterType="com.chedao.websocket.webserver.user.model.UserTypeEntity" useGeneratedKeys="true" keyProperty="id">
		insert into user_type
		(
			`user_id`,
			`name`,
			`build_time`

		)
		values
		(
			#{userid},
			#{name},
			#{buildtime}
		)
	</insert>

    <update id="update" parameterType="com.chedao.websocket.webserver.user.model.UserTypeEntity">
        update user_type
        <set>
            <if test="name != null">`name` = #{name}</if>
        </set>
        where id = #{id}
    </update>


    <delete id="delete">
		delete from user_type where id = #{value}
	</delete>

    <!--<delete id="deleteBatch">
        delete from user_info where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>-->

</mapper>